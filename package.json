{
  "name": "smartpark360-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd admin-dashboard && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd admin-dashboard && npm start",
    "build": "cd admin-dashboard && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
